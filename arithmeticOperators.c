#include <stdio.h>

int main() {

  int x = 2;
  int y = 3;
  int z = 0;
  int a = 4;

  // Addition
  // z = x + y;
  // Subtraction
  // z = x - y;
  // Multiplication
  // z = x * y;
  // Division
  // z = a / x;
  // Modulus
  z = y % x;

  printf("%d\n", z);

  return 0;
}
